# Changelog

## [1.2.0] - 2026/08/27

### Added

- `build-playbook` スキルを新規追加。`review-contract` スキルが参照する交渉プレイブック（`playbook.md`）を新規構築・更新する。モードA（新規構築）／モードB-1（更新・改訂: 規程・方針ドリブン）／モードB-2（更新・改訂: レビュー実績ドリブン）の3モードを定義。B-1 と B-2 は併用可能とし、併用時は改訂案を統合して提示する。**モードB-1 はモードAと同じ6系統すべてを収集対象とする**（改定された規程だけに範囲を狭めない。改訂内容の妥当性は雛形・実績・事例・レビュー記録でも裏付ける必要があるため）。
- 根拠情報の収集を6系統（① ヒアリング（必須） / ② 社内規程・既存基準書・契約書雛形 / ③ 過去契約・交渉実績 / ④ インシデント・トラブル事例 / ⑤ 法令・強行規定 / ⑥ レビュー実績・版更新記録）に整理し、優先順位を「ヒアリングで明示された自社方針 > 社内規程 > 過去実績 > 一般的な商慣行」（法令上の強行規定は常に最優先）と定義。
- ③ 過去契約・交渉実績の取得経路を契約管理システムに限定せず、(a) 契約管理システム / CLM系コネクタ、(b) 契約管理台帳・スプレッドシート、(c) 過去契約のファイル添付、(d) メールコネクタ、(e) 口頭聴取の5経路に対応。経路ごとに集計の信頼度が異なるため、根拠に**出所と件数の併記を必須化**し、件数を提示できない情報は「過去実績」ではなく「ヒアリング」として記録する。台帳と契約本文が食い違う場合は本文を優先する。
- ④ インシデント・トラブル事例の系統を新設。**事例そのものを書き写さず原因を条項レベルの条件に変換**して反映する対応表（条項の欠落 → §2.x 追加 / 基準の甘さ → 許容できない条件の厳格化 / エスカレーション漏れ → §1.2・§3 RED / 相手方要求の受諾 → §4 Tier 1 昇格 / 契約外の運用問題 → 反映しない）を定義。③（許容範囲を広げる方向）と ④（厳格化する方向）が矛盾する場合は ④ を優先して厳格化を提案する。
- ⑥ レビュー実績・版更新記録の分析（モードB-2）を追加。`list_task` / `list_comment`（`resource_type="TASK"`）/ `download_attachment` / `download_document` で記録を収集し、条項単位の論点に分解・集約したうえで、9種の観測パターン（ルールの欠落 / 標準文言化できる修正 / 基準外での妥結 / プレイブックと異なる判断 / 指摘の取消 / RED のエスカレーション漏れ / 毎回フォールバックで妥結 / 相手方要求により条件を戻した記録 / 往復回数の突出）に分類して改訂候補を導出する。判定は同一論点3件以上を既定とし、1〜2件は「観測」として継続観察に留める（重大類型は1件でも提案）。
- ② に**契約書雛形**を追加。雛形から、扱っている契約類型の特定（条件付き質問の選択に使用）、標準条項の構成、条項内の数値（事実上の自社基準）、雛形に無い条項（スコープ漏れ）、立場別の雛形差、最終更新時期（陳腐化）、雛形が無い類型（交渉力の弱さ）を抽出する。雛形の記載を自動的に自社基準として確定することと、相手方提供の雛形を自社基準の根拠にすることを禁止した。
- **プレイブックの品質点検を独立モードにせず、出力前セルフチェックとして A / B 共通で必須化**（ステップ5.2）。点検区分は欠落・判定不能・矛盾・運用の実効性の4つ。全節を対象とし（モードBでも今回改訂しなかった節を含む）、指摘は「今回の作成・改訂に起因するもの」（出力前に自分で修正）と「以前から残っているもの」（継続課題としてファイル出力のみ、承認フローには載せない）に分ける。残る2区分（陳腐化・実績と事例の乖離）は改訂の必要性の検出であり、B-1（法令条番号の突合）と B-2（レビュー実績の分析）に統合した。
- **「棚卸ししたい」「今の基準で問題ないか確認したい」という依頼をモードBとして受け付ける経路を追加。** 改訂対象が指定されていなくてよく、**改訂候補が0件なら本文を変更せず「改訂不要」と報告して終了**できる（点検した範囲と結果をファイルに残し、次回見直し予定を更新する）。
- **出力の互換性要件**として、生成するプレイブックのセクション番号（§1〜§6）を固定。`review-contract` がレビューコメントに「プレイブック §2.1 に基づき」の形で節番号を引用するため、条項の追加は §2.7 以降に採番し、廃止時も番号を再利用しない。§3 の重大度ラベルを変更する場合は `review-contract` の `comment-format.md` と揃える必要がある旨も明記。
- **プレイブック冒頭にメタデータブロックを必須化**（版数・最終更新日時・承認者・承認日・次回見直し予定・対象範囲）。エージェントはセッションを越えて状態を保持しないため、**最終更新日時をプレイブック本文に記録し、これを改訂時の情報収集の起点とする**。内容を変更した出力では必ず更新し、§6 改訂履歴の最新行の日付と一致させる。節番号を持たないブロックとすることで §1〜§6 の採番に影響を与えない。あわせて §6 改訂履歴を必須とした。
- 「推測禁止の原則」を定義。基準値には根拠（ヒアリング / 社内規程 / 過去実績 / インシデント事例 / 法令）の明示を必須とし、根拠のない項目は `※要確認`、商慣行に基づく暫定値は `※要確認（一般的な商慣行に基づく暫定値）` として確定値と区別する。実績の最頻値を自動的に自社基準としない、実績1〜2件を一般化しない等のガードレールを明記。
- プレイブックに取引先名・案件名・個人名を残さない**匿名化要件**と、エスカレーション先・承認者を役割名で記載するルールを定義。
- **ユーザーへの質問・判断依頼をすべてフォーム形式に統一**（`references/interaction-format.md`）。番号で答えられる選択肢を用意し、各問に必ず「保留（後で決める）」を置いて `※要確認` に落とす。関連項目は1フォームにまとめてよいが上限7問とし、異なるセクション・契約類型・立場の質問を1フォームに混ぜない。8問以上になる場合は記入用フォームをファイルで配布する（`assets/interview-form.md`）。
- **質問文の自己完結を必須化。** チャット上では引用元をたどれないため、現行プレイブック・社内規程・契約書・レビュー記録・法令の引用、出典参照（規程名・条番号・節番号・ファイル名・事例の識別子）、「上記の」等の指示語を質問に含めることを禁止。現状に触れる場合は引用ではなく内容を言い換えた事実を1文で示し、判断材料は件数などの数値のみを添える。
- **次の行動をユーザーに選ばせない運用。** スキルが次の1手を宣言し「進める / 修正する / 中断する」の承認のみを求める承認フォームを定義。
- **進行の単位を「1つずつ完結」に固定。** セクション順に確定させ、契約類型が複数ある場合は件数の多い順に1類型ずつ完結（2つ目以降は確定済みと同じでよい項目を明示し差分のみ質問）、立場が分かれる場合は立場ごとに完結。取引形態ごとにプレイブックを分ける場合も1つを完成させてから次に着手する。
- **人がレビューしやすいWord出力に対応。** ステップ6で常に3ファイルを出力する: `playbook.docx`（見出しスタイル・表・目次を適用し、`※要確認` を太字＋赤字で強調。スキル側ではコメント・変更履歴を書き込まない）、`review-sheet.csv`（決定事項の一覧。`対応`（承認/修正/保留）と `コメント` 列を空欄で用意。**Wordが使えない場合のフォールバック**）、`playbook.md`（`review-contract` 連携用）。Markdownのみを渡してレビューを依頼することと、どの形式でレビューするかをユーザーに質問することを禁止した。
- **レビュー観点の明示を必須化。** 「全体を確認してください」という依頼を禁止し、その回の作成内容に応じて観点を3点程度に絞って提示する。
- **フィードバックの機械的な回収を追加。** `scripts/extract_review_feedback.py`（標準ライブラリのみ）で、レビュー済み `.docx` のWordコメント（指摘本文と対象箇所の対応付け）・変更履歴（挿入/削除）・表、およびレビューシート `.csv` の記入内容を抽出する。`対応` 列は承認/修正/保留/未記入に正規化し、表記揺れを吸収する。Excel が付与するUTF-8 BOM と Shift_JIS の両方に対応。目視での読み取りを禁止した。
- **初回・新規構築時に「用意すると良いインプット」を提示する手順を追加**（ステップ1.2、モードA・B-1で必須）。契約審査基準、契約書雛形、決裁規程、過去契約、契約管理台帳、トラブル事例集、情報セキュリティ規程、レビューメモの8種について、所在・効果・無い場合の代替を明示する。すべて任意でヒアリングのみでも作成できること、優先すべき2種（決裁規程とトラブル事例集）、形式を問わないこと、部分的でよいこと、所要の目安を伝える。「必要な資料を提出してください」という丸投げと、資料が揃うまで着手を保留することを禁止した。
- **ヒアリングを「決定項目」で定義。** 質問文と選択肢をスキルに列挙せず、決めるべき項目（何を決めるか）と回答スケール（S1 要否 / S2 可否 / S3 数値 / S4 個別）だけを持つ構成とした（`interview-guide.md`）。契約類型13種・規制9領域は「追加で決めること」の一覧に集約し、質問の生成はモデルの契約実務知識に委ねる。ただし**自動的には想起しにくく抜けると法令違反に直結する規制フック**（下請法の適用判定・支払期日・書面交付、フリーランス法の取引条件明示、労働者派遣法の抵触日通知と偽装請負の回避、個人情報の越境移転の根拠と報告責任の所在、独占禁止法の再販売価格拘束・競合品制限、建設業法の一括下請禁止、輸出管理の該非判定、業規制、贈収賄防止、OSSライセンス）は明示的に保持する。
- **7ステップのワークフロー**（モードの選択と着手準備 / 現行プレイブックの取得 / 根拠情報の収集と分析 / スコープの確定と基準のヒアリング / 生成・セルフチェック・承認 / レビュー用ファイルの出力とFB取り込み / 運用への引き渡し）。**ステップ番号と実行順序が一致**しており、スコープと分割の確定は §2 の基準を聞く前に行う。
- 評価シナリオを `evals/build-playbook.md` に追加（5シナリオ＋スクリプトの動作確認項目）。`src/` の外に置き、配布バンドルには含めない。
- **プレイブックを適切な大きさに保つための分割規則を定義**（`playbook-schema.md`「6. 適切な大きさに保つ」）。プレイブックはレビュー時に毎回読まれる文書であり、小さすぎれば判断できず、大きすぎれば読まれない。**基準を削って小さくするのではなく、必要な基準を書いたうえで大きくなったら分割する**方針とした。分割のトリガーは「§2 の条項の過半で類型ごとの場合分けが3つ以上並ぶ」「全体が通読の実用限界（目安 A4 20ページ / 概ね1,000行）を超える」「類型ごとに承認者・改訂サイクルが異なる」のいずれか。**分割の最小単位は契約類型**とし、立場（委託者/受託者）では分割せず同一類型内で書き分ける（ファイル数が倍になり共通部分の同期コストが釣り合わないため）。分割後の各ファイルは §1〜§6 を持つ自己完結の文書とし（`review-contract` は1回のレビューで1ファイルを読む前提）、共通部分（§1・§3〜§5）は全ファイルで同一に保ち、共通部分の改訂は全ファイルへ反映する。ファイル名は `playbook-<類型>.md`、扱う類型と立場はメタデータの「対象範囲」に明記し、全ファイルを合わせて漏れなく重複なくカバーする。分割の判断は §2 のヒアリングに入る前に行う（着手後の分割は節番号と共通部分の整合を取り直すことになるため）。
- **法令の条文・数値をプレイブックに転記しない規則を定義。** 「法令の下限を満たすこと」と**適用されるかの判定条件**のみを書き、数値は「e-Gov」法令検索コネクタで都度取得する。理由は分量ではなく**法改正で誤りになるため**。ヒアリングでも法令要件の数値をユーザーに決めさせず、「法令の要件を満たす前提でよいか」と自社の追加要件のみを確認する。追加する条項・基準は自社の取引実態と実績・事例に裏付けがあるものに限り、「あった方がよい」だけの一般的な観点は増やさない。スキルの参照ファイル自体にも法令解説を蓄積しない。
- `build-playbook` はカスタマイズせずそのまま利用する前提とし、しきい値・改訂頻度などをスキル内の既定値として確定（定期見直しは年1回、改訂候補は同一論点3件以上など）。組織固有の運用はスキルの編集ではなくヒアリングで聴取して反映する。
- `skills/build-playbook/references/` に `interaction-format.md`（対話フォーマット規約）、`playbook-schema.md`（メタデータブロック・既定セクション構成・判定可能性の要件・根拠の記載方法・匿名化要件）、`evidence-sources.md`（準備インプットの案内、6系統の収集フロー、雛形からの抽出観点）、`interview-guide.md`（決定項目・回答スケール・規制フック）、`review-output.md`（レビュー用出力の体裁・観点の絞り方・FB取り込み）、`update-policy.md`（改訂トリガー・差分提示・出力前セルフチェック・レビュー実績の分析・改訂履歴）を追加。参照ファイルは6点に集約し、姉妹スキルとの粒度差を縮小（3,356行/12ファイル → 2,315行/10ファイル）。
- `skills/build-playbook/scripts/extract_review_feedback.py`（Wordコメント・変更履歴・レビューシートの抽出）を追加。
- `skills/build-playbook/assets/` に `interview-form.md`（一括フォームの記入用雛形）と `playbook-template.md`（プレイブックの骨格テンプレート。汎用ダミー）を追加。

### Changed

- `.copilot-bundle/manifest.json` の `agentSkills` に `./skills/build-playbook` を追加。
- README.md に `build-playbook` スキルの概要・ディレクトリ構成・カスタマイズ方針を追記。
- `review-contract` スキルに「未確定の基準（`※要確認`）の扱い」を追加。`build-playbook` が付す `※要確認` の項目について、推測で補わず一般的な商慣行・法令をベースラインとして判定し、その旨をコメントの【修正の根拠】に明記する、エスカレーション基準が未確定なら RED をすべて報告対象とする、レビュー結果の報告時に未確定だった基準を一覧で伝える、というルールを定義。従来は `※要確認` を含むプレイブックを渡された場合の挙動が未定義だった。
- `review-contract` スキルのプレイブック参照方法に、**プレイブックが契約類型ごとに分割されている場合の選択手順**を追加。レビュー対象の契約類型と自社の立場に対応するプレイブックを1つ選んで使用し、複数を混在させて参照しない（基準が重複・矛盾するため）。判断できない場合はユーザーに確認し、該当するものが無い場合はその旨を分析結果に明記する。

### Fixed

- **プレイブックのファイル構成をユーザーに質問していた問題を修正。** 実機で「プレイブックのファイル構成をどうしますか」という質問が出ていた。分割の判断を着手前の承認フォームで求める手順になっており、「ユーザーに構成を質問しない」という基本原則と矛盾していた。分量が確定していない段階では答えられず、ファイル構成という技術的判断を法務担当者に求めることにもなる。**分割の判定を出力時（ステップ6.1）に移し、スキルが分量を測って判定して結果を1文で報告する**方式に変更した（可否を問うフォームは出さない）。着手前に決める必要がないのは、§2.1〜2.6 の番号が全ファイルで同一、共通部分（§1・§3〜§5）も同一内容のため、後から分割しても採番のやり直しやヒアリングの聞き直しが発生しないため。基本原則の見出しを「ファイル構成をユーザーに質問しない」に改め、プラグインの構成に加えて**プレイブックのファイル構成・分割の有無**も対象に含めた。`interaction-format.md` の禁止事項にも同項を追加し、回帰確認用の評価シナリオ（4b）を追加した。

- **`playbook.docx` の生成手段が未定義だった問題を修正。** ランタイムの文書生成機能で作成すること（`review-contract` と同じ手段。生成用スクリプトは同梱しない）と、生成できない環境ではレビューシートと `playbook.md` の2ファイルで進める縮退動作を明記。従来は .docx を必須としながら生成手段も依存関係も示していなかった。
- **一括フォームの発動条件が逐次進行を無効化していた問題を修正。** 「確認事項が8件以上」というしきい値ではモードAの確認事項（共通で100問超）が常に該当し、セクション単位の逐次進行が発動しなかった。条件をモード基準（モードAは原則配布、モードBは20件以上）に変更し、配布後も矛盾・欠落・重い判断は逐次確認する旨を明記。あわせて「8問以上」（1フォームの設問数）と「8件以上」（未確定の総数）で同じ数値を別義に使っていた記述を整理。
- **ステップ5とステップ6の循環依存を解消。** 5が6の結果を、6が5の結果を要求しており番号順に実行できなかった。§1.1の棚卸し → ステップ6でスコープ確定 → §1.2以降のヒアリングという実行順序を明示。
- **同梱サンプルを現行版として改訂しかねない誤判定を修正。** 未カスタマイズ判定が「サンプルの注記が残り**かつ**基準値がプレースホルダ」のAND条件で、説明用に具体値を持つ同梱サンプルでは成立しなかった。注記の有無のみで判定する条件に変更。
- **最終更新日時が改訂の起点として機能しない問題を修正。** 毎出力で更新する規則のため、レビュー往復で再出力するたびに前進し、次回改訂の収集期間が「前回の実質的な改訂時点」ではなく「前回の再出力時点」になっていた。更新するのは承認された改訂の確定時のみとし、承認前は据え置く規則に変更。あわせて改訂履歴も「承認された改訂ごとに1行」とし、往復での行の増殖を防止。起点の定義を `playbook-schema.md` の1箇所に集約し、他ファイルからは参照のみとした（従来は4箇所で言い換えており、レビュー実績分析側では「前回改訂日」と別定義になっていた）。
- **選択肢6つ以内という自ルールへの違反を解消。** 上限は単一選択の設問に適用し、複数選択のチェックリスト（取引形態の棚卸し、除外事由の列挙など、網羅的に挙げること自体に意味がある設問）は対象外と定義。該当する10設問に「複数選択可」を明記。
- **`interview-form.md` を書式仕様に整理。** 従来は共通48問を列挙していたが、その46%が決定項目ガイドの選択肢と実質同一の二重管理だった。設問の列挙をやめ、記入方法・設問の書式・セクションの並び・立場別の配布方法・回収後の扱いだけを持つ構成に変更（300行→76行）。設問はその回の対象範囲に応じて決定項目から組み立てる。
- **規制領域「贈収賄防止・コンプライアンス」への到達経路がなかった問題を修正。** 条件付き質問の対応表に該当行がなく、9領域のうち1領域が実質的に使われなかった。官公庁・公的機関との取引や海外公務員が関与する取引をトリガーとする行を追加。あわせて秘密保持・対価/支払条件の追加条項への導線も追加。
- **既定条項に秘密保持と対価・支払条件が含まれていなかった問題に対応。** §2.1〜2.6 の番号は互換性のため変更せず、両条項を §2.7 以降の追加候補として明示し、追加条項の要否の設問に含めた。
- `scripts/extract_review_feedback.py` の3件の想定漏れを修正。`Ok` のような表記が正規化されず承認件数が過少になっていた（大文字小文字・全角空白・末尾句点を吸収する正規化に変更し、対応表に無い値は `unrecognized` として計上）。Excel の既定保存形式である `.xlsx` が非対応だった（標準ライブラリのみで SpreadsheetML を読む実装を追加。共有文字列・空欄セルの詰まりに対応）。Wordの文字列移動（`w:moveFrom` / `w:moveTo`）が無警告で欠落していた（移動として抽出し件数を計上）。あわせて破損XMLでトレースバックが素通しになる問題、複数段落コメントで語が連結される問題、決定事項の行番号が表ごとに振り直される問題、UTF-16 CSV が文字化けする問題を修正し、決定事項に節番号を含めるようにした。
- 節番号保持の対象が §1〜§5 で、必須の §6 が漏れていた記述を修正（`update-policy.md`）。
- 所要の目安「共通40問程度」を実測値（100問程度）に修正（`evidence-sources.md`）。
- 「セクション0〜6がプレイブックの §1〜§6 に対応」という記述を修正（セクション0は情報源の確認で対応する節を持たない）。
- 法令の条番号チェックにコネクタ非接続時の代替（「条文未確認」として指摘に残し推測しない）を追記。
- 承認者を役割名で記載する規則について、監査対応で個人単位の証跡が必要な場合は承認ワークフローや稟議記録に残す旨を追記。
- プレイブックの参照範囲を決める設問について、フォールバックとエスカレーション基準が交渉上の機密である旨の注意喚起を追加。

### Removed

- 各リファレンスの「カスタマイズガイド」節を削除（`build-playbook` はカスタマイズせずそのまま利用する前提のため）。しきい値・改訂頻度はスキル内の既定値として確定した。

## [1.1.4] - 2026/08/27

### Changed

- `review-contract` スキルの「関連コメントの取得」で使用するコメント取得ツールを、非推奨の`list_task_comment`から`list_comment`に差し替え（`SKILL.md`）。`list_comment`は対象リソースの指定（`resource_type` / `resource_id`）が任意で、指定しなければタスクと契約書を横断して取得する仕様のため、**ツール名だけを差し替えると意図せず横断取得になる**。レビュータスクのコメントを取得する箇所では`resource_type="TASK"`と`resource_id`（対象タスクのID）の指定を明記した。コメント添付ファイルIDの取得元を示す注記も同様に差し替え。あわせて「ContractS CLM」MCPでは`list_task_comment`／`list_document_comment`が非推奨となり`list_comment`に統合された（旧ツールの動作は維持される）。コメント一覧の応答には条件に合致する総件数（`total_count`）が含まれるようになったが、本スキルでは未使用。

## [1.1.3] - 2026/08/13

### Changed

- `review-contract` スキルの「参照契約書の取得」を「関連契約・参照契約書の取得」に改題し、取得手順を拡張。非推奨の`search_references`を`search_documents`に差し替え、新たに手順4として`get_related_contracts`による関連契約ツリー展開（基本契約→個別契約→変更契約書＝変更履歴）を追加。渡す契約IDは手順3で選択した参照契約書のもの（`search_documents`のレスポンスに含まれる contractId）に限定し、レビュー対象自身の契約IDは不要である旨を明記（未締結のレビュー対象は契約IDを取得できず、方法Bで判明するのは documentId のため）。本文で明示的に参照されていない兄弟契約や過去の変更覚書も漏れなく把握し、レビューのインプットとする。`get_related_contracts`が関連契約を返さない場合は親子関係未登録の可能性がある旨をレビュー所見に含める注意事項を追記。
- `draft-contract` スキルの入力ソース取得（モードA・原契約検索）で使用する非推奨の`search_references`を`search_documents`に差し替え（`SKILL.md`および`references/input-sources.md`）。
- `draft-contract` スキルの後続フロー・カスタマイズ例（ContractS CLM 利用時）における原契約への紐付けツールを、非推奨の`relate_contracts`から`set_parent_contract`に差し替え。文言を「原契約（親）に子として紐付ける」に調整。
### Fixed
- `draft-contract` スキルの後続フロー「原契約への紐付け」に、**締結完了が前提である**ことを明記（`SKILL.md` ステップ10）。文書ID（documentId）と契約ID（contractId）が別体系であり、契約IDの取得手段が検索インデックス経由に限られ、インデックス登録が締結完了時にのみ行われるため、ドラフト登録直後は紐付けできない。従来はこの前提が書かれておらず、登録直後に「親子紐付けも行いますか？」と提案して契約ID取得に失敗する動作になっていた。登録直後に紐付けを提案してはならない旨、締結完了後に実行できる旨を案内する手順に修正。あわせて、**CLMの画面からも締結済みでなければ親子登録できないため締結前の回避策は存在しない**ことを明記し、画面操作を回避策として案内しないよう禁止。あわせてカスタマイズ例を「登録まで」と「締結完了後の紐付け」に分割し、`search_documents`の検索対象が`ORIGINAL`カテゴリの締結済み契約に限られるため**変更覚書（`MEMORANDUM`判定）は締結後でも自身の契約IDを取得できず、画面操作の案内が必要**である制約を追記。

## [1.1.2] - 2026/08/10

### Added

- `review-contract` スキルのレビュープロセス「3. 全体像の把握」に、契約書の添付ファイルの取得手順を追加。方法B（ContractS CLMからの取得）の場合のみ、`list_attachment_files`ツールで契約書に添付されたファイルの一覧を取得し、ファイル名から内容を推定してレビューに有用な資料（見積書、仕様書、SOW、相手方からの修正依頼、交渉経緯資料など）の候補をユーザーに提示、選択されたファイルを`download_attachment`ツールでダウンロードしてレビューのインプットに追加するフローを定義。契約書本文と添付資料の矛盾・不整合は指摘事項に含める。

### Changed

- `review-contract` スキルの「関連コメントの取得」を拡張。`list_task_comment`のレスポンスに`attachmentFiles`（`attachmentFileId` / `attachmentFileName`）が含まれる場合、`download_attachment`ツールでダウンロードしてレビューのインプットに追加する手順を追加（方法Bの場合のみ）。
- 添付ファイル取得の制約・注意事項を明記。`download_attachment`のダウンロードURLは有効期限が300秒（5分）で期限切れの場合は再取得する、`list_attachment_files`はコメントに添付されたファイルを含まない、契約書の閲覧権限がない場合は一覧が空・ダウンロードが403となる、契約書・契約書コメント・作成依頼タスクコメント以外のコメントの添付ファイルは404となり再試行しない。
- README.md の `review-contract` 機能一覧に添付ファイル連携を追記。

## [1.1.1] - 2026/07/24

### Changed

- `review-contract` スキルのレビュープロセス「3. 全体像の把握」に、関連コメントの取得手順を追加。方法B（ContractS CLMからの取得）の場合のみ、`list_task_comment`ツールでレビュータスクに紐づくコメント（交渉経緯、レビュー依頼背景、注意事項など）を取得し、レビューのインプット情報として活用するフローを定義。
- `review-contract` スキルに「5. レビュー完了コメントの投稿」セクションを新規追加。方法B（ContractS CLMからの取得）の場合のみ、レビュー完了後に`post_task_comment`ツールでタスクの依頼者に対してレビュー完了コメントを投稿するフローを定義。コメント案をユーザーに提示し投稿前の確認を必須化。既存の「5. レビュータスクの完了」は「6. レビュータスクの完了」に番号を繰り上げ。

## [1.1.0] - 2026/07/17

### Added

- `draft-contract` スキルを新規追加（refs #7116）。原契約や雛形を参照し、変更覚書を代表ユースケースとした法務文書ドラフト（.docx）を生成する。入力ソースの3択（① コネクタ取得 / ② ファイル添付 / ③ 直接入力）、原契約からの正確な引用と変更前後の対比、推測禁止・`※要確認` 明示・ドラフト注記のガードレールを定義。
- 対象類型: 変更覚書 / 各種覚書・合意書 / NDA / 業務委託等の定型契約 / 発注書・注文書 / 通知書・同意書。文書類型の判別はモデルの知識で行い、類型ごとの構成ヒントは `generation-rules.md` に集約。
- 生成モードを「既存契約の一部変更（変更覚書等）」と「雛形からの新規作成」の2つに明確化。既存契約の変更時は、コネクタ接続時に対象契約を検索して特定する手順を定義（ContractS CLM 利用時は `search_references` で候補検索）。
- 生成後の後続フロー（任意・コネクタ接続時）を定義: ドラフトのCLM登録（登録先フォルダはユーザーに確認）→ レビュー依頼 → 原契約への関連契約紐付け。スキル本体は製品非依存の記述とし、ContractS CLM の具体ツール名（`list_folder` / `get_upload_url` / `create_document_from_uploaded_file` / `relate_contracts` 等）はカスタマイズ例として分離。
- `skills/draft-contract/references/` に `input-sources.md`（入力ソース選択フロー）、`generation-rules.md`（生成アプローチ・ガードレール・類型別の構成ヒント）、`review-criteria.md`（専門家レビューの要否判定基準）を追加。
- 雛形（テンプレート）は利用者の保管場所（① コネクタ: Google Drive / Salesforce / CLM 等）から取得する設計とし、同梱の `assets/` はサンプル（例）と位置づけ（必要に応じて自社雛形を配置可）。`amendment-format.md` は廃止し、変更覚書で雛形が無い場合は同梱の `assets/amendment-template.docx` を差し込みベースに使う。
- `skills/draft-contract/scripts/fill_docx.py`（python-docx による雛形差し込み）と、`skills/draft-contract/assets/amendment-template.docx`（汎用ダミーの変更覚書雛形）を追加。

### Changed

- `plugin.json` の説明文を、契約書レビューに加えて法務文書ドラフト生成を含む内容に更新。バージョンを 1.1.0 に更新。
- README.md にドラフト生成スキルの概要・ディレクトリ構成・カスタマイズ手順・プライバシー/免責を追記。

## [1.0.9] - 2026/07/09

### Changed

- `SKILL.md` のレビュープロセス「3. 全体像の把握」に、参照契約書の取得手順を追加。レビュー対象の契約書が基本契約等の他の文書を参照している場合、`search_references`ツールでContractS CLM上の候補を検索し、ユーザー選択後に`download_document`でダウンロードしてレビューのインプットに追加するフローを定義。

## [1.0.8] - 2026/05/27

### Added

- `references/comment-format.md` を新規作成。Wordコメント（吹き出し）挿入に特化したガイドライン（挿入位置のルール、含める情報、フォーマット例、禁止事項）を定義。

### Changed

- `SKILL.md` のステップ8を再構成。基本方針・変更履歴付き修正案の書き込み・禁止事項はSKILL.mdに残し、コメント挿入のフォーマットは `references/comment-format.md` を参照する構成に変更。

## [1.0.7] - 2026/05/25

### Changed

- GitHub公開リポジトリ（contracts-inc/legal-plugin）での配布に向け、単独リポジトリに移行。
- README.mdを追加し、セットアップ手順・配布方法・カスタマイズガイドを整備。

## [1.0.6] - 2026/05/22

### Changed

- `contract-review` スキルの構成をリファクタリング。SKILL.mdを普遍的なレビュー手順（プロセス）に特化し、各社固有の基準（チェックポイント、許容範囲、重大度分類、交渉優先順位など）を `references/playbook.md` に分離。
- CLM登録時（ケースA・ケースB共通）に、レビュー前の文書からの変更意図および要点を4,000文字以内で詳細にまとめた更新コメントを付与するステップを追加。

### Added

- `references/playbook.md` に各社でカスタマイズ可能なサンプルプレイブックを新規作成。基本方針、類型別チェックポイント（自社基準/許容できない条件/フォールバック）、リスク重大度分類、交渉優先順位、修正案作成ガイドライン、カスタマイズガイドを含む。

## [1.0.5] - 2026/05/12

### Changed

- `contract-review` スキルのレビュープロセスにユーザー確認ステップ（Step 9）を追加。総合評価完了後、Wordファイル作成に進む前にレビュー結果をユーザーに提示し承認を得ることを必須化。
- Wordファイル作成時の指摘事項の記載方法を厳格化。すべての指摘は既存文書の該当箇所にWordコメント（吹き出し）として挿入し、文書末尾へのサマリー追加を禁止。

## [1.0.4] - 2026/05/12

### Added

- レビュータスクの完了機能を追加。ContractS CLMから取得した契約書のレビュー完了後、`complete_task`ツールでレビュータスクを完了にするステップを追加。

### Changed

- 契約書ダウンロード時のパラメータを明確化（`type: "docx"`, `finalized_review_download: false`を明示）。
- 契約書ダウンロードとファイル確認のステップを1つに統合。

## [1.0.2] - 2026/05/11

### Added

- ContractS CLM連携機能を追加。`list_task`によるレビュータスク一覧取得、`download_document`による契約書ダウンロード機能を実装。
- レビュー対象の契約書取得方法として「方法A: ユーザーによるファイル添付」と「方法B: ContractS CLMからの取得」の2パターンを定義。
- レビュー結果のContractS CLMへの登録機能を追加（`update_document_version`によるバージョン更新、`create_document_from_uploaded_file`による新規登録）。

### Changed

- プラグインの説明文を「ContractS CLMと連動して自社基準と法令に基づく契約書レビューを実施するスキル」に変更。
- MCP設定にContractS CLM（`contracts-clm`）を追加。
- スキル名を`contract-review`に変更し、説明文にCLM連携の記述を追加。

## [1.0.1] - 2026/05/07

### Changed

- Gmail MCPのURLを`https://gmailmcp.googleapis.com/mcp/v1`に更新。

## [1.0.0] - 2026/03/24

### Added

- Legalプラグイン初版リリース。
- `contract-review`スキルを追加。組織の交渉プレイブックに基づく契約書レビュー、リスク重大度分類（GREEN/YELLOW/RED）、修正案（レッドライン）生成機能を実装。
